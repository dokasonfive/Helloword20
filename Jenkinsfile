pipeline {
    agent any
    stages {
        stage('build'){
           steps{
              echo "building the project we want to build on Sunday"
              sleep 10
            }
        }
    }
}
